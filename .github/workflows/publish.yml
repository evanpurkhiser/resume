name: Build and Publish Resume

on:
  push:
    branches:
      - main

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip

          # Download and install Helvetica
          wget https://font.download/dl/font/helvetica-neue-5.zip -O helvetica.zip
          unzip -o helvetica.zip -d helvetica-fonts
          sudo mkdir -p /usr/share/fonts/truetype/helvetica
          sudo cp helvetica-fonts/*.ttf /usr/share/fonts/truetype/helvetica/ 2>/dev/null || true
          sudo cp helvetica-fonts/*.otf /usr/share/fonts/truetype/helvetica/ 2>/dev/null || true
          sudo fc-cache -f -v

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Build project and generate PDF
        run: npm run build:pdf

      - name: Delete existing release
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete latest --yes
          git push origin :refs/tags/latest || true

      - name: Create latest release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest Resume
          body: |
            Automatically generated resume build from the latest commit.

            **Commit:** ${{ github.sha }}
            **Date:** ${{ github.event.head_commit.timestamp }}
          files: |
            dist/resume.pdf
            dist/index.html
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
